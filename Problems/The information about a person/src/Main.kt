import java.util.Scanner

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    // put your code here
    println("${scanner.next().first()}. ${scanner.next()}, ${scanner.nextInt()} years old")
}