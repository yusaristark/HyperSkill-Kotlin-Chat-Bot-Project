import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)

    // write your code here
    println("${scanner.nextInt()}:${scanner.nextInt()}:${scanner.nextInt()} ${scanner.nextInt()}/${scanner.nextInt()}/${scanner.nextInt()}")
}
